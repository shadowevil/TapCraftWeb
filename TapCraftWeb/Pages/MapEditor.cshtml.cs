using Microsoft.AspNetCore.Mvc.RazorPages;

namespace TapCraftWeb.Pages;

// Isometric map editor. Reachable only by navigating directly to /MapEditor;
// it is intentionally not linked from anywhere in the UI.
public class MapEditorModel : PageModel
{
    public void OnGet()
    {
    }
}
